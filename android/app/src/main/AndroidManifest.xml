<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Firebase(Auth·Firestore) 통신에 필요하다. debug/profile 매니페스트는 핫 리로드용으로
         이미 선언하지만 그건 그 두 빌드 타입에만 적용되므로, release APK에서도 켜지려면
         여기에 있어야 한다. 카메라 권한은 mobile_scanner 플러그인 매니페스트가 선언한다. -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- 만료 임박 로컬 알림(flutter_local_notifications).

         POST_NOTIFICATIONS: Android 13(API 33)부터 알림은 런타임 권한이다. 선언만으로는
           부족하고 앱이 실행 중에 요청해야 한다(ExpiryNotificationSync가 로그인 직후 1회).
         RECEIVE_BOOT_COMPLETED: 예약 알람은 **재부팅하면 전부 사라진다.** 아래 부팅
           리시버가 이 권한으로 깨어나 예약을 복원한다. 없으면 사용자가 폰을 한 번 껐다
           켜는 것만으로 알림이 조용히 멈춘다.

         SCHEDULE_EXACT_ALARM은 **일부러 선언하지 않는다.** Android 12+에서 별도 권한
         화면을 거쳐야 하는데, "만료 3일 전 오전 9시"가 Doze로 수십 분 밀리는 것은
         무해하다(스케줄러가 inexactAllowWhileIdle을 쓴다). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <application
        android:label="KeepCon"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true"> //로컬 에뮬레이터(10.0.2.2) 및 HTTP 통신 허용을 위한 설정
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- 초대 링크(App Links). 이게 없으면 링크가 브라우저로 새고 앱은 열리지 않는다.

                 android:autoVerify="true" — 설치 시 시스템이
                 https://<host>/.well-known/assetlinks.json 을 받아 이 앱의 서명 지문과
                 대조한다. 통과해야 "확인된 링크"가 되어 선택 다이얼로그 없이 바로 열린다.
                 ⚠️ 검증에 실패하면 조용히 브라우저로 열린다 — 지문이 맞는지는
                 `adb shell pm get-app-links com.keepcon.app`으로 확인한다.

                 host는 Group.inviteHost·firebase.json의 hosting과 **셋이 같아야** 한다.
                 pathPrefix로 /invite만 받는다(호스팅의 다른 페이지까지 앱이 가로채지
                 않도록). -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data
                    android:scheme="https"
                    android:host="keepcon-ab660.web.app"
                    android:pathPrefix="/invite"/>
            </intent-filter>
        </activity>
        <!-- 만료 임박 알림 수신부(flutter_local_notifications).

             ScheduledNotificationReceiver: 예약 시각에 깨어나 알림을 실제로 띄운다.
             ScheduledNotificationBootReceiver: 재부팅·앱 업데이트 후 예약을 복원한다.
               android:exported="false" — 외부 앱이 이 리시버를 호출해 가짜 알림을
               띄우지 못하게 막는다(시스템 브로드캐스트만 받으면 된다). -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"
            android:exported="false"/>
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
